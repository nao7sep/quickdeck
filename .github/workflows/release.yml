name: Release

# Per the app-release-conventions: test gate -> per-OS package -> attach to a
# DRAFT release. CI stays minimal — the .dmg / setup.exe / portable-zip packaging
# lives in scripts/package.sh (macOS) and scripts/package.ps1 (Windows), which run
# `tauri build`. Nothing is published automatically; a draft is created for the
# pushed tag, verified, then published by hand (gh release edit --draft=false).
#
# Windows can't be cross-compiled from a Mac, so both platforms build in CI.
#
#   - push a tag `v*` -> build both platforms, attach installers + portables to a
#     single draft release for that tag.
#   - manual dispatch -> build only (no release), for a smoke check.

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # Gate on both supported platforms: the frontend suite (including manifest
  # version consistency) plus the Rust core's own tests. Both build jobs depend
  # on every matrix leg.
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
      - run: npm install --global npm@12.0.2
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - name: Rust tests
        working-directory: src-tauri
        run: cargo test

  build:
    needs: test
    strategy:
      fail-fast: false # a Windows failure never cancels the macOS build
      matrix:
        include:
          - os: macos-latest
            package: bash scripts/package.sh
          - os: windows-latest
            package: pwsh scripts/package.ps1
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target
      - run: npm install --global npm@12.0.2
      - run: npm ci

      # One command per OS — runs `tauri build` and emits the installer + portable
      # into artifacts/.
      - name: Package (host OS)
        run: ${{ matrix.package }}

      - name: Attach to draft release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3.0.1
        with:
          draft: true
          # NOT a pre-release. "Pre-release" versus "release" is a subjective
          # label that buys no real latitude — it reads as an excuse rather than a
          # fact — so a 0.x version number carries that signal on its own. The draft
          # flag is what actually protects the release: nothing goes public until it
          # is inspected and published by hand.
          prerelease: false
          # Body renders from the pushed tag (${{ github.ref_name }}), so the note
          # never goes stale on a version bump — no release-specific text baked in.
          body: |
            **QuickDeck ${{ github.ref_name }}**

            These builds are unsigned, so the OS warns on first launch:
            - **macOS**: right-click the app → **Open** (or `xattr -dr com.apple.quarantine QuickDeck.app`)
            - **Windows**: SmartScreen → **More info** → **Run anyway**
          files: |
            artifacts/*.dmg
            artifacts/*-setup.exe
            artifacts/*-mac.zip
            artifacts/*-win.zip
